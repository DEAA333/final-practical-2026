# Laravel 13 Practical Exam — Student Repository

Target: **Laravel 13 / PHP 8.3+**

```bash
composer install
cp .env.example .env
php artisan key:generate
touch database/database.sqlite
php artisan migrate:fresh --seed
php artisan serve
```

Accounts: `admin@example.com`, `ahmed@example.com`, `sara@example.com`, `omar@example.com`, `lina@example.com` — password: `password`.

Run: `php artisan test`
