# Laravel 13

This repository targets Laravel 13 and PHP 8.3+.

Install dependencies with:

```bash
composer install
```

Verify:

```bash
php artisan --version
```

Expected major version:

```text
Laravel Framework 13.x
```

The exam code intentionally contains incomplete/incorrect sections for students to debug and extend.
